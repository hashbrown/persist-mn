---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Supporting Minnesota businesses that support humankind' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <div class="min-h-screen flex flex-col">
      <slot />
      <footer class="mt-auto bg-gray-800 text-white py-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <p class="text-center text-gray-300">
            © {new Date().getFullYear()} Persist MN. Supporting Minnesota businesses that are actively helping their community.
          </p>
        </div>
      </footer>
    </div>
  </body>
</html>
